# Copy to .env or .env.local for Vite hub dev
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Leave unset for manual tier grants via Supabase profiles.tier (recommended until Worker is deployed).
# Set only when gojito-backend is live and you grant Guac via POST /api/admin/entitlements:
# VITE_GOJITO_API_URL=http://127.0.0.1:8787
